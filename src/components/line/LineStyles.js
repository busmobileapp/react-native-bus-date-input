import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#7b92a7',
        borderBottomColor: '#7b92a7',
        borderBottomWidth: 1
    },
    customContainer: {
        backgroundColor: '#7b92a7',
        borderBottomColor: '#7b92a7'
    }
});

export default styles;